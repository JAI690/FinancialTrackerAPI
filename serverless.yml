service: FinancialTrackerAPI
frameworkVersion: '2 || 3'

provider:
  name: aws
  runtime: nodejs12.x
  lambdaHashingVersion: '20201221'
  region: 'us-east-2'
  iam:
    role:
      statements:
      - Effect: Allow
        Action:
                - dynamodb:Query
                - dynamodb:Scan
                - dynamodb:GetItem
                - dynamodb:PutItem
                - dynamodb:UpdateItem
                - dynamodb:DeleteItem
        Resource: arn:aws:dynamodb:us-east-2:537778070242:table/users-FTAPI



resources:
  Resources:
    UsersTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: users-FTAPI
        BillingMode: PAY_PER_REQUEST
        AttributeDefinitions:
          - AttributeName: email
            AttributeType: S
        KeySchema:
          - AttributeName: email
            KeyType: HASH



functions:
  hello:
    handler: handler.hello
    events:
      - httpApi:
          path: /
          method: get
  signup:
    handler: users.signup
    events:
      - httpApi:
          path: /signup
          method: post
  signin:
    handler: users.signin
    events:
      - httpApi:
          path: /signin
          method: post   
  cognito:
    handler: users.cognito
    events:
      - httpApi:
          path: /cognito
          method: get
  signin2:
    handler: users.signin2
    events:
      - httpApi:
          path: /signin2
          method: get  
